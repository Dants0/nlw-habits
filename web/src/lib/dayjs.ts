import dayjs from "dayjs";
import 'dayjs/locale/pt-br'

dayjs.locale('Pt-br');

// export function capitalizeFirstLetter(str:string) {
//     const letter = str.charAt(0).toUpperCase() + str.slice(1);

//     return letter
// }

// // export const result = capitalizeFirstLetter(dayjs.locale('Pt-br'))
// console.log(capitalizeFirstLetter(dayjs.locale('Pt-br')))

// // console.log(result)
